# Как настроить сохранение результатов в лидерборд

## ✅ Что уже сделано:

1. **Игра теперь отправляет результаты в API** - при окончании игры результат автоматически отправляется на сервер
2. **API для лидерборда готов** - `/api/leaderboard` обрабатывает сохранение и получение результатов
3. **Подключение к кошельку** - используется wagmi/RainbowKit для получения адреса игрока
4. **Индикаторы на экране** - игра показывает статус сохранения и подсказывает подключить кошелек

## 🔧 Что нужно настроить:

### 1. Создать Vercel KV базу данных

Если вы еще не создали KV базу:

1. Откройте ваш проект на [vercel.com](https://vercel.com)
2. Перейдите в раздел **Storage**
3. Нажмите **Create Database**
4. Выберите **KV (Redis)**
5. Создайте новую базу данных
6. После создания, Vercel покажет вам переменные окружения

### 2. Настроить переменные окружения

**Для локальной разработки:**

1. Откройте файл `.env.local` в корне проекта
2. Раскомментируйте строки с переменными
3. Вставьте значения из Vercel:
   ```
   KV_REST_API_URL="https://your-url.kv.vercel-storage.com"
   KV_REST_API_TOKEN="your_token_here"
   KV_REST_API_READ_ONLY_TOKEN="your_readonly_token_here"
   ```

**Для продакшена (на Vercel):**

Vercel автоматически добавит эти переменные в ваш проект при деплое.

### 3. Запустить проект

```bash
npm run dev
```

## 🎮 Как работает:

1. **Игрок подключает кошелек** - нажимает "Connect Wallet" в интерфейсе
2. **Играет в игру** - управляет персонажем
3. **Игра заканчивается** - результат автоматически отправляется в лидерборд
4. **Результат сохраняется** - если это лучший результат игрока, он сохраняется в базе данных

## 📊 Что сохраняется:

- **Адрес кошелька** - уникальный идентификатор игрока
- **Лучший результат** - только если новый результат лучше предыдущего
- **Timestamp** - время достижения результата

## ❗ Важно:

- Результаты сохраняются **только если подключен кошелек**
- Сохраняется **только лучший результат** каждого игрока
- В лидерборде хранится **топ-100 игроков**

## 🐛 Отладка:

Если результаты не сохраняются, проверьте:

1. **Консоль браузера** - откройте Developer Tools (F12) и посмотрите на ошибки
2. **Переменные окружения** - убедитесь что `.env.local` настроен правильно
3. **Подключение кошелька** - убедитесь что кошелек подключен перед игрой
4. **Сеть Somnia** - убедитесь что кошелек подключен к правильной сети

## 📝 Тестирование:

```bash
# Запустить dev сервер
npm run dev

# Открыть в браузере
# http://localhost:3000/game

# 1. Подключить кошелек
# 2. Сыграть в игру
# 3. Проверить консоль на наличие сообщения:
#    "New high score saved to leaderboard!"
```

## 🔗 Полезные ссылки:

- [Vercel KV Documentation](https://vercel.com/docs/storage/vercel-kv)
- [wagmi Documentation](https://wagmi.sh/)
- [RainbowKit Documentation](https://www.rainbowkit.com/)
